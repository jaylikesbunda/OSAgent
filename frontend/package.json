{
  "name": "osagent-frontend",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --dev",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
